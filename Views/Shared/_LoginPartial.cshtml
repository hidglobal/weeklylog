@if (Request.IsAuthenticated)
{
    <text>
        <ul class="nav navbar-nav navbar-right">
            <li class="navbar-text">
                @User.Identity.Name
            </li>
        </ul>
    </text>
}
